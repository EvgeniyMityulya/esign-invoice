# Screenshots

Drop real app screenshots here (PNG), then swap the mock in index.html.

In index.html, replace the whole `<div class="screen"> ... </div>` mock with:
    <img src="./shots/sign.png" style="width:100%;display:block" alt="Signing a document">

Suggested files: sign.png (signing), invoice.png, reports.png.
